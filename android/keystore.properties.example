# Copy to keystore.properties (gitignored) and fill in, or run ./make-release-key.sh.
# Required to build a release-signed APK (buildTypes.release in app/build.gradle.kts).
storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=androidbridge
keyPassword=CHANGE_ME
