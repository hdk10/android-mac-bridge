name = "android-bridge-relay"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Durable Object that holds the Mac's WebSocket per pairing-id.
[[durable_objects.bindings]]
name = "ROOMS"
class_name = "Room"

# Free plan requires SQLite-backed Durable Objects.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
