{
  "name": "android-bridge-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object relay for android-bridge (off-LAN notification forwarding)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.103.0"
  }
}
